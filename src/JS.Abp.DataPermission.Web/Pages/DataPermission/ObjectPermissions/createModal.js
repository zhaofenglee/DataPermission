var abp = abp || {};

abp.modals.objectPermissionCreate = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("DataPermission");
        
        
        
        
        
    };

    return {
        initModal: initModal
    };
};
